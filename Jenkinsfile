
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
          
		    echo "${BUILD_NUMBER}"
		    echo "${TAG}" 
            }
        }
        stage('Deploy') {
            steps {
                sh '''
			ls
			
		'''
            }
        }
    }
}

